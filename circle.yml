machine:

  timezone:
    America/Los_Angeles

  java:
    version:
      oraclejdk8

test:
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - find . -type f -regex ".*/target/surefire-reports/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/junit/ \;
    - mkdir -p $CIRCLE_TEST_REPORTS/site/
    # debug
    - find . -type d -regex ".*/target/site"
    # end debug
    - cp -r target/site/* $CIRCLE_TEST_REPORTS/site/